description: Something went wrong
content:
  application/json:
    schema:
      type: object
      required:
        - message
      properties:
        message:
          type: string
          example: "Whoops: unexpected error happened"
